import { HighlightOverDUEPipe } from './highlight-over-due-pipe';

describe('HighlightOverDUEPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightOverDUEPipe();
    expect(pipe).toBeTruthy();
  });
});
